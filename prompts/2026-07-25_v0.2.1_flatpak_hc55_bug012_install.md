---
date: 2026-07-25
repo: SteamDeckMSX_Native
status: open
resume: "verder met SteamDeckMSX_Native Flatpak op HC55 — BUG-012-fix (share-tree + C-BIOS install + OPENMSX_SYSTEM_DATA) gebouwd; bij groene bundle: hosten op /steam/ voor Deck-install; daarna restscope v0.2.2 (auto-setMachine, commandFinished, slot B-floppy, MIME-detect, tests-backfill, ROM-header)"
---

# v0.2.1-KingsValley — Flatpak HC55: BUG-011 bevestigd opgelost, BUG-012 install-fix

- Trigger "verder met konami" → open sessie 24-7 opgepakt (die eindigde zonder OEU midden in flatpak-build-debugging op HC55, `/root/sdmsx_build`)
- Vondst: buildlog 24-7 08:36 UTC toont volledige openMSX-compile + link ✅ → **BUG-011 opgelost** (RCA retroactief in BUGLIST.md gezet — commits verwezen ernaar maar entry ontbrak)
- Nieuwe faalstap = **BUG-012 (geel)**: manifest installeerde uit `derived/*/bindist/` (bestaat alleen bij `make bindist`); bovendien te weinig (alleen machines+skins) én DATADIR-mismatch in sandbox
- Fix in `nl.icthorse.SteamDeckMSX.yaml`: hele `share/.` → `/app/share/openmsx/`, `Contrib/cbios` XML's→machines/ + ROM's→systemroms/ (P-SDM-05: C-BIOS), finish-arg `--env=OPENMSX_SYSTEM_DATA=/app/share/openmsx`; layout gematcht met `OpenmsxLocator::discoverDataPath()` kandidaat 2 (`/app/share/openmsx`, machines/ direct eronder)
- Volgende actie: rsync manifest → HC55, `run-build.sh` herstart (bundle-stap zit erin), bij exit 0 → `SteamDeckMSX-v0.2.1-KingsValley.flatpak` hosten op /steam/
