---
date: 2026-05-31
repo: SteamDeckMSX_Native
status: done
resume: ""
---

# Sessie 2026-05-31 (v0.0.5-Solid Snake) — XML-stream-parser + BUG-004 + machine-keuze

**Agent:** Claude Opus 4.7 (1M context)
**Repo:** SteamDeckMSX_Native (`cpaglebbeek/SteamDeckMSX_Native`)
**Branche:** main
**Cross-repo werk:** geen
**Eindstand commits:** (volgt aan einde sessie)

---

## Opdracht (samengevat)

Gebruiker zei "verder, v0.0.5" + "ja B" → Scope B (medium, geen Stap 21): BUG-004 + XML-stream-parser + C-BIOS machine-keuze. D-pad iconen + save-states + Stap 21 schuiven naar v0.0.6.

Codenaam Solid Snake = Metal Gear 2: Solid Snake (Konami MSX2 1990) — narratieve cirkel met Meta-repo v0.0.1 Metal Gear.

---

## Prompts en acties — chronologisch

### Prompt 1 — "verder, v0.0.5"
**Actie:** WhatIf v0.0.5 met 3 scope-opties (A=Stap 21, B=medium zonder Deck, C=alles). Advies B met 10 sub-beslissingen.

### Prompt 2 — "ja B"
**Actie:** Scope B uitgevoerd:

1. **OpenmsxLocator uitbreiden** — `dataPath` property + 4-laagse auto-discovery (Mac bindist / Flatpak / Linux dev). Dev-fallback paths gefixt naar 3 relative-root-levels.
2. **MsxCore — env-var setting** — `QProcessEnvironment OPENMSX_SYSTEM_DATA` zet vóór elke spawn.
3. **MsxCore — XML-stream-parser** — `QXmlStreamReader` incremental, `addData(chunk)` per onReadyRead. PrematureEndOfDocument graceful. Parse-handlers voor `<reply>`/`<update>`/`<log>`/`<openmsx-output>`.
4. **MsxCore — IPC-signals** — `replyReceived(int, bool, QString)`, `stateUpdate(QString, QString, QString)`, `logMessage(QString, QString)`, `rawLine(QString)`. `sendCommand()` retourneert command-id (counter-based).
5. **MachineModel nieuw** — QAbstractListModel met dynamic loading via `machine_info machines` Tcl-call. Tcl-list-parser voor space-separated + `{brace-quoted}` namen. Fallback hardcoded C-BIOS-lijst. Persist current in QSettings.
6. **MachineSelector.qml nieuw** — inline bottom-bar dropdown, Popup-boven-control. Focus-nav, A=select, B=close. ● dot voor current.
7. **Main.qml updates** — MachineModel + dataPath propagation + onStateChanged Running → refresh machine-list + compact openmsx-path display.
8. **Tests uitgebreid 6 → 10** — T7 (reply parsing), T8 (update parsing), T9 (log parsing met history-zoek), T10 (state-transitions). Mock-script-helper schrijft `.sh` files met XML-dumps.

### BUG-004 live-validatie

Direct command-line verificatie op Mac met onze submodule openMSX:
```
OPENMSX_SYSTEM_DATA=<bindist/Contents/Resources/share> openmsx --version
→ openMSX 21.0
  flavour: opt
  components: CORE GL LASERDISC
```
✅ Bevestigt dat onze auto-discovery + env-var-setting werkt op Mac dev-fallback. Op Steam Deck Flatpak heeft het issue niet (paths impliciet via /app/share/openmsx).

---

## Belangrijke keuzes deze sessie

| Keuze | Reden |
|---|---|
| **QXmlStreamReader incremental** | Stream-friendly, handelt buffer-half-elements goed via PrematureEnd-error |
| **Command-id counter** in MsxCore | Eenvoudig, deterministic; reply-correlatie via signal-args |
| **MachineModel via signal-binding** ipv direct `core->machines()` | QML-vriendelijk + losse coupling |
| **Fallback hardcoded machine-lijst** | Eerste-launch ervaring niet-leeg; vervangen zodra dynamic slaagt |
| **Tcl-list-parser eigen** ipv QXmlStream | reply-body is plain Tcl-list, niet XML — eigen char-by-char depth-tracker |
| **MachineSelector inline ipv aparte settings-screen** | Steam Deck D-pad-friendly; één-knops-toegang vanuit cartridge-browser |
| **Codenaam Solid Snake** | Konami MSX2 1990, narratieve sequel van Meta-repo v0.0.1 Metal Gear |

---

## BUILD-OUTCOME — Mac smoke v0.0.5

| Stap | Resultaat |
|---|---|
| Clean rebuild met Qt 6.11.1 | ✅ 38/38 targets |
| App start (offscreen) | ✅ `SteamDeckMSX 0.0.5-SolidSnake target= native` |
| Geen QML-warnings, geen errors | ✅ |
| Tests 10/10 (incl. XML-parser cases) | ✅ alle pass |
| BUG-004 live-validatie | ✅ env-var werkt, openMSX 21.0 + components |
| Initieer MachineModel via QSettings | ✅ fallback-lijst direct beschikbaar |

**Conclusie:** XML-parser werkt, BUG-004 gefixt, machine-keuze functionaal. Klaar voor Steam Deck-deploy in v0.0.6.

---

## Open eindjes na deze sessie

**v0.0.6 (eerstvolgende sessie):**
- **D-pad SVG iconen** in `src/assets/icons/` — 8 vector-files
- **Save-state slot-grid** — `savestate "<name>"` + `loadstate "<name>"` commands; UI 10-slot grid met thumbnails (later)
- **Stap 21 — eerste Flatpak-build op Steam Deck**: vereist SSH-info gebruiker (skip-loop tot 4e poging)
- **MachineSelector D-pad-keyboard-hint** in bottom-bar — uitleg "+Y/X = Stop/menu"

**Onafhankelijk:**
- Stream_Server v0.0.2 (andere sessie) — Sunshine op HC55

**Open architectuurvragen:**
- ROM-load reply-handling: huidige `loadRom` stuurt `carta` cmd maar wachten we op reply vóór UI-update? v0.0.6
- Auto-detect machine bij ROM-load (heuristic op file naam) → setCurrentMachine vóór start()

---

## Verbinding met andere sessies

| Sessie | Locatie | Verbinding |
|---|---|---|
| v0.0.4-Aleste | Native/prompts/...v0.0.4...md (status: done) | MsxCore state-machine basis; XML-parser is v0.0.5 evolutie |
| v0.0.3-Castlevania | Native/prompts/...v0.0.3...md (status: done) | UI-fundament + Tokens singleton |
