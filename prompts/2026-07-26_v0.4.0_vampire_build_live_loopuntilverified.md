---
date: 2026-07-26
repo: SteamDeckMSX_Native
status: done
resume: "verder met SteamDeckMSX — v0.4.0-Vampire is GEBOUWD en LIVE (build-all.sh volledig groen, beide gates, bundle + deploy.sh op HC55). Volgende stappen: (1) QML-fouten fixen die /loopuntilverified vond: OnlineBrowser.qml:45:17 en EmulatorMenu.qml:42:13 'Unable to assign [undefined] to int' + 2x 'Could not attach Keys property' — én het foutfilter in deploy/build-all.sh regel 56 uitbreiden met 'Unable to assign|Could not attach' (gate was blind voor deze klasse); (2) de online download (v0.5.0-werk) een keer via de UI doorlopen; (3) install op de Deck zelf (eerst flatpak uninstall --user!). Open: BUG-023 Steam Input-preset, BUG-025 build-cache (openmsx-miss opnieuw bevestigd, 9m02s), save-state terugladen"
---

# v0.4.0-Vampire — build volledig groen, live op HC55 + /loopuntilverified-rapport

## Trigger

"verder met steamdeck msx" — resume-punt van gisteren: v0.4.0-Vampire + v0.5.0-werk zaten in git maar waren nog nooit gebouwd; op HC55 draaide v0.3.5.

## Wat er gebeurd is

1. **WhatIf + akkoord** ("Ja, volledige keten"), daarna `./deploy/build-all.sh` in de achtergrond met monitor op de stap-voortgang.
2. **Alle 7 stappen groen**: versie-check (0.4.0-Vampire consistent), lokale build, 6/6 tests, offscreen app-start, rsync, Flatpak-build op HC55 (9m02s, gemeten 04:39:33→04:48:35), beide release-gates (flatpak-gate 4/4 + zichtbaarheids-gate: galerij → spel schermvullend → F12 terug → openMSX kon schrijven, 8 bestanden).
3. **Live**: `SteamDeckMSX-v0.4.0-Vampire.flatpak` (27.815.048 bytes) + VERSION + latest-symlink op https://horsecloud55.ddns.net/steam/flatpak/ — installeren op de Deck: `curl -fsSL https://horsecloud55.ddns.net/steam/flatpak/deploy.sh | bash`.
4. **`/loopuntilverified`** op de build-voortgangsmelding: 3 iteraties, fixed point, 22 beweringen, 100% verifieerbaar. Rapport gemaild (msg-id 19f9cc73da40cd5c, attachment geverifieerd); werkmap `~/.claude/loopuntilverified/20260726T044704Z/`.

## Bevindingen uit de verificatielus (actiepunten!)

- **🟡 Stap 4 "geen QML-fouten" was te sterk**: run.log bevat 2× `Unable to assign [undefined] to int` (`OnlineBrowser.qml:45:17`, `EmulatorMenu.qml:42:13`) en 2× `Could not attach Keys property` (OnlineBrowser + EmulatorMenu zijn geen Item). Het grep-filter op regel 56 van `deploy/build-all.sh` dekt alleen `is not a type|Cannot assign|SyntaxError`. **Fix beide**: de QML-properties zelf én het filter uitbreiden.
- **❌ "Open sessies: geen tekenen"** klopte niet — 8 andere claude-resume-processen + externe MEMORY.md-wijziging. Les: open-sessies-claim altijd meten met procescheck, niet alleen git (conform feedback_parallelle_sessie_detectie).
- **BUG-025 opnieuw bevestigd**: 2 modules cache-hit, openmsx rebuild; buildduur daardoor 9m02s. Spoor `derived/` uit de bronmap blijft open.

## Niet gedaan (bewust)

- De online downloadflow (v0.5.0-werk) is deze sessie NIET via de UI doorlopen — sessie afgesloten met OEU vóór dat punt.
- Deck-install: onveranderd open; feedback via ClaudeBug-foto + `/bugcheck`.
